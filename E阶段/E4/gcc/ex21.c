int_fast16_t 的含义是：
至少能存储 16 位有符号整数，并且是在当前平台上运算速度最快的整数类型。

它不一定正好是 16 位。例如某台 64 位机器使用 64 位整数更快，那么 int_fast16_t 可能就是 64 位。
简单区分：
int16_t：必须恰好 16 位。
int_least16_t：至少16位，选择占空间最小的。
int_fast16_t：至少16位，选择运算最快的。
普通 int：通常就是 CPU 比较自然、常用的整数类型。
